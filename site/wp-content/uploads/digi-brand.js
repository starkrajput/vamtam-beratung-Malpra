/* Digicomplish — header state is set statically:
   inner pages carry body.digi-page (solid white header, dark logo/text),
   the home page keeps the template's transparent header over the dark hero.
   No runtime logic is required. */
